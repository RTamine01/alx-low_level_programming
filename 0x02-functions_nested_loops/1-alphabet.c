#include "main.h"

/**
 * entry - point
 * print alphabt in lower case
 * Return: always 0 (success0)
 */

void print_alphabet(void);
{
	char letter'a';
	while (letter <= 'z')
	{
		_putchar(letter);
		letter++;
	}

	_putchar('\n');
}
int main(void)
{
	print_alphabet();
	return (0);
		}
